[package]
name         = "cadence-nnue"
description  = "Reserved for Cadence's quantised NNUE inference; empty today"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
authors.workspace      = true
repository.workspace   = true

[lib]
name = "cadence_nnue"

[features]
default = ["simd"]
# Stub. Gates nothing; present so the feature name the SIMD paths will be
# switched by is reserved, and so the default is already the one they want.
simd    = []

[dependencies]
cadence-core = { path = "../core" }

[lints]
workspace = true
